rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This project uses an Express backend with the Firebase Admin SDK.
    // Deny all direct browser/client access and let the backend own reads/writes.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
